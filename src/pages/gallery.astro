---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const galleryItems = [
  {
    src: 'https://lh3.googleusercontent.com/aida-public/AB6AXuBnZx-BSnNWyDcU10TMoUwqQCJUr0xKsx8Wm8r4-Gg47bKdqcNNUTTruiIi84vOYX5NVzYgHLqN6LifqukJOxoV_CDNTJnemVee33tVWWmHR8ps_ygzgyXWx6sbL3FyXURoNGB76IwlLV1VzUSca3UKI1EFwEEH_vYzjSRKfdSUc74nkn27wQqrixbY-jswysJMMFssBZadoUtJGqJqb4c-j4jDruSLxh-c5tMaTQrqdMiEZC7oZg09_q1m4FG9_DDSEjAnyvAppjE',
    alt: 'Superior Room - Casa Roca Canggu',
    caption: 'Superior Room',
    category: 'rooms',
    cols: 8,
    height: 'h-[500px]',
  },
  {
    src: 'https://lh3.googleusercontent.com/aida-public/AB6AXuAs3jECiGwHnBcaPyQKR_HhBZbbPBUuc4IruEN-LrrP7bSphTrKHGKtuyDg_SpBtYm68HT-bJa2ChWi9GLkrUH_NP7pi7Wdn1mjw9AJUkAq1uZ12rT7pUe7U7YY_YTodUOKJqPbxk_kJ9Bsvi3x4gdBvytZvqBNvIq9OyHzlBLPQjLNrS8R1TMrt0Z1ARPIL_uHpYQRyV1BxoSjc56MpYgBke-8j8UNjrTg-cFO8iLCPgLTZgF84TK3IQFOJWanG--Ud3PNZt4hp-0',
    alt: 'Teak Craftsmanship - Casa Roca Canggu',
    caption: 'Teak Craftsmanship',
    category: 'details',
    cols: 4,
    height: 'h-[500px]',
  },
  {
    src: 'https://lh3.googleusercontent.com/aida-public/AB6AXuD_SpGX64-4isKLfbZ3d3Gd__6E42k8sqmSAKOA2zLkry8VBdLu4GtIzNTeWEteVq8cvBC5N3BPlxMoHvlH7KrcnTG9Gub6Eo8Hsumm9HrmCwuYWZBIaGtSTd8xrWdewenmR1rlx3_a5g9MtdfPejWof14MEJbQg-8XH6knQEJN-lPam4MP1JXu5ilDjnJeZzc7NfclXIVrUjp5Cxoy5sY2LrzeqYY8Y2Dwt6w5mJKzzVNYGmLsQst_j29fSl8wOxaerZ2OYbs53RU',
    alt: 'Batu Bolong Beach at golden hour',
    caption: 'Batu Bolong Sands',
    category: 'around',
    cols: 4,
    height: 'h-[350px]',
  },
  {
    src: 'https://lh3.googleusercontent.com/aida-public/AB6AXuCr69Iu_ODkEmRUHrozx3MFH5l150_L0P8dCn13PbdLdSFD4kczWjBcP0jhF4V07WkPAfsqF1hlEqL-6pW2EdrQ-nK_F7WNzUEeoYQt4T5DpqGGvEAZDkB6KxpyExYVvgJM4e9wuL-TTg3fncq6CdmPlMq6MSRMGyApUKCdI5j-3UZVWGzR-DNZ1nxGU--bLTFv_vdDgSXqK68eIjg5CH8a30k2pT0sdtPQWRDMsAMFFCW9ZhptxoCRjYFSzvVnRdDUcsBdTAX518I',
    alt: 'Casa Roca private balconies exterior',
    caption: 'Private Balconies',
    category: 'rooms',
    cols: 8,
    height: 'h-[350px]',
  },
  {
    src: 'https://lh3.googleusercontent.com/aida-public/AB6AXuBWVzDBM1VDznaBnESekQagtIwcMSB3_GDEXXmXq9RsZk3KHmXs9QjpVVktBSZjqguSt_olwwZOuSJA1nujMP6BcuTfbMH_jTbilC1YARSe9NXLsZXLlbxoWXzQq4T__Ne8orm_Q4bpOlWaoMzzIlkb8di3EUzuCxrguvOtH31ihp0fGMlKlAUHxJl8mFbpHVnPurXY08JNW8QGIA5MVDXMUagTw9pnrsH64kddsx0wseOV-ywP5Z9Lwm8alN-L4XqfMsJGpx0_tF4',
    alt: 'Deluxe Room interior Casa Roca',
    caption: 'Deluxe Living',
    category: 'details',
    cols: 4,
    height: 'h-[500px]',
  },
  {
    src: 'https://lh3.googleusercontent.com/aida-public/AB6AXuAZHsm5Be1efEhWJiOSRhTGwJqWwk6SYb-r94E8zmcZj2ImhmVQqblQFBwBHFezUFhJzZmNnGmQRbq69Gy6deEzu4oxzDOZ3gP9jIp02U73696vPaxvwOtF3RlMYMKLfIkuzKLYj2zvMgrAkqjbp8SQvE6bHQXslJUwqg0uIP3fBPCvuQ2_K2HiIXW7wANcfKUFuB4c-VRhutRSC-pcK4cKdmaSEsdLnRoD76lB5OASYRXKdk1q_I5dUgs1JIist3KEaU7IQL2HbMs',
    alt: 'Casa Roca sanctuary atmosphere',
    caption: 'The Sanctuary',
    category: 'details',
    cols: 8,
    height: 'h-[500px]',
  },
  {
    src: 'https://lh3.googleusercontent.com/aida-public/AB6AXuBp8Mzsxm8jbudU-jht1w4_yDYI96kJB1pPJ_9iQiXdY2DOxdQ5zT37SNKzFakkcd2_mRFL2uMgT-MK82p3yHucAu8M2JmlTFlEp-w3_FwQJ6dihIBHDuwZw2NH1OVYHGLuMNfdMiXcy-6eGF79wtkhAm3zOSrdaGgBdbS5_HocAvCdNSbaLw7oGmmij_KDcdLDpbCRuaFL8akALcekkuLIIxpNPqsh1uM_nPs8-CuWJfE_fUZrRuMwvhb92DciqYnviyCgq-FSjxY',
    alt: 'Boutique cafe in Canggu Batu Bolong',
    caption: 'Canggu Culture',
    category: 'around',
    cols: 6,
    height: 'h-[400px]',
  },
  {
    src: 'https://lh3.googleusercontent.com/aida-public/AB6AXuCSio3RvLHczV2MykpPuFpSnXvg11zf1Bc4HTTt4JI7dbGcJYDWtWdcaJqwglFZhh1dD8bCj4d3udcefiq9syhlhBgxe-mwOl6-El3uExgIrwMXJSMwDOpNeenCtEcJrRcChJChLTsFOTZ_etfVBoJD6aRxwvtqT0HXGeYvaI0xtUunWXYVqNqVfMSaSBBtMNdstcAZIb7SUQMCht-VVdhNHzETIqC5FMf3qAR0OPek5_0KhU5SncUbL72Ox9Mv1VW0Ry-lc2yqh6c',
    alt: 'Stone basin bathroom detail Casa Roca',
    caption: 'Honed Textures',
    category: 'details',
    cols: 6,
    height: 'h-[400px]',
  },
];
---

<BaseLayout
  title="Gallery Casa Roca Canggu | Artisanal Sanctuary in Bali"
  description="Browse photos of Casa Roca Canggu — Superior and Deluxe rooms, architectural details, and the Batu Bolong neighbourhood."
  canonical="https://casaroca.com/gallery"
>
  <Header />

  <main class="max-w-container-max mx-auto px-margin-mobile md:px-margin-desktop">

    <!-- Header -->
    <header class="pt-24 pb-16 md:pt-32 md:pb-24">
      <div class="max-w-3xl">
        <h1 class="font-headline-xl-mobile md:font-headline-xl text-headline-xl-mobile md:text-headline-xl text-on-surface mb-8">
          Gallery - Artisanal Sanctuary in Canggu
        </h1>
        <p class="font-body-lg text-body-lg text-on-surface-variant leading-relaxed">
          A careful dialogue between structure and nature. Casa Roca Canggu is defined by its
          architectural intentionality, featuring solid teak furniture and rough-hewn stone
          finishes. Nestled near the vibrant pulse of Batu Bolong, our sanctuary remains a silent
          retreat for those who seek tactile luxury and a serene sense of place.
        </p>
      </div>
    </header>

    <!-- Filters -->
    <div class="flex flex-wrap gap-8 mb-12 border-b border-surface-variant" id="filters">
      <button class="filter-btn active pb-4 font-label-md text-label-md uppercase tracking-widest transition-all" data-filter="all">All</button>
      <button class="filter-btn pb-4 font-label-md text-label-md uppercase tracking-widest text-on-surface-variant hover:text-secondary transition-all" data-filter="rooms">Rooms</button>
      <button class="filter-btn pb-4 font-label-md text-label-md uppercase tracking-widest text-on-surface-variant hover:text-secondary transition-all" data-filter="details">Details</button>
      <button class="filter-btn pb-4 font-label-md text-label-md uppercase tracking-widest text-on-surface-variant hover:text-secondary transition-all" data-filter="around">Around Canggu</button>
    </div>

    <!-- Gallery Grid -->
    <div class="grid grid-cols-1 md:grid-cols-12 gap-gutter" id="gallery-grid">
      {galleryItems.map((item, index) => (
        <div
          class={`gallery-item md:col-span-${item.cols} group overflow-hidden rounded-xl relative cursor-zoom-in`}
          data-category={item.category}
          data-index={index}
        >
          <img
            src={item.src}
            alt={item.alt}
            class={`w-full ${item.height} object-cover transition-transform duration-700 group-hover:scale-105`}
            loading="lazy"
          />
          <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 flex flex-col justify-end p-8">
            <span class="font-label-sm text-label-sm text-white uppercase tracking-widest">{item.caption}</span>
          </div>
        </div>
      ))}
    </div>

    <!-- CTA -->
    <section class="mt-section-gap mb-section-gap py-20 bg-surface-container-low rounded-xl px-8 text-center relative overflow-hidden">
      <div class="absolute inset-0 opacity-10 bg-gradient-to-br from-secondary to-transparent pointer-events-none"></div>
      <div class="relative z-10 max-w-2xl mx-auto">
        <h2 class="font-headline-lg text-headline-lg text-on-surface mb-6">Experience the Sanctuary</h2>
        <p class="font-body-md text-body-md text-on-surface-variant mb-10">
          Your journey to the heart of Canggu begins here. Secure your artisanal escape and
          rediscover the luxury of silence.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/availability" class="bg-secondary text-on-secondary px-10 py-4 rounded-lg font-label-md text-label-md hover:opacity-90 transition-all hover:-translate-y-1 active:translate-y-0">
            Check Availability
          </a>
          <a href="/rooms" class="border border-secondary text-secondary px-10 py-4 rounded-lg font-label-md text-label-md hover:bg-secondary/5 transition-all">
            View Room Rates
          </a>
        </div>
      </div>
    </section>

  </main>

  <Footer />

  <!-- Lightbox -->
  <div id="lightbox" role="dialog" aria-modal="true" aria-label="Image viewer">
    <button id="lightbox-close" aria-label="Close lightbox">
      <span class="material-symbols-outlined" style="font-size:32px">close</span>
    </button>
    <button id="lightbox-prev" aria-label="Previous image">
      <span class="material-symbols-outlined" style="font-size:40px">chevron_left</span>
    </button>
    <img id="lightbox-img" src="" alt="" />
    <button id="lightbox-next" aria-label="Next image">
      <span class="material-symbols-outlined" style="font-size:40px">chevron_right</span>
    </button>
    <div id="lightbox-caption"></div>
  </div>

</BaseLayout>

<style>
  .filter-btn.active {
    color: #77574d;
    border-bottom: 2px solid #77574d;
  }
  .gallery-item {
    transition: opacity 0.3s ease, transform 0.3s ease;
  }
  .gallery-item.hidden-item {
    opacity: 0;
    transform: scale(0.96);
    pointer-events: none;
    position: absolute;
    visibility: hidden;
  }

  /* Lightbox */
  #lightbox {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 100;
    background: rgba(0, 0, 0, 0.92);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    align-items: center;
    justify-content: center;
  }
  #lightbox.open {
    display: flex;
  }
  #lightbox img {
    max-width: 90vw;
    max-height: 85vh;
    object-fit: contain;
    border-radius: 8px;
    box-shadow: 0 32px 80px rgba(0, 0, 0, 0.5);
    transition: opacity 0.2s ease;
  }
  #lightbox-caption {
    position: absolute;
    bottom: 32px;
    left: 50%;
    transform: translateX(-50%);
    color: rgba(255, 255, 255, 0.6);
    font-family: Inter, sans-serif;
    font-size: 12px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    white-space: nowrap;
  }
  #lightbox-close {
    position: absolute;
    top: 24px;
    right: 24px;
    color: white;
    cursor: pointer;
    opacity: 0.7;
    transition: opacity 0.2s;
    background: none;
    border: none;
  }
  #lightbox-close:hover { opacity: 1; }
  #lightbox-prev,
  #lightbox-next {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    color: white;
    cursor: pointer;
    opacity: 0.6;
    transition: opacity 0.2s;
    padding: 16px;
    background: none;
    border: none;
  }
  #lightbox-prev:hover,
  #lightbox-next:hover { opacity: 1; }
  #lightbox-prev { left: 16px; }
  #lightbox-next { right: 16px; }
</style>

<script>
  // ─── Filter ───────────────────────────────────────────────────────────────
  document.querySelectorAll('.filter-btn').forEach((btn) => {
    btn.addEventListener('click', () => {
      const filter = btn.dataset.filter;

      document.querySelectorAll('.filter-btn').forEach((b) => {
        b.classList.remove('active');
        b.classList.add('text-on-surface-variant');
      });
      btn.classList.add('active');
      btn.classList.remove('text-on-surface-variant');

      document.querySelectorAll('.gallery-item').forEach((item) => {
        const match = filter === 'all' || item.dataset.category === filter;
        item.classList.toggle('hidden-item', !match);
      });
    });
  });

  // ─── Lightbox ─────────────────────────────────────────────────────────────
  const lightbox  = document.getElementById('lightbox');
  const lbImg     = document.getElementById('lightbox-img');
  const lbCaption = document.getElementById('lightbox-caption');
  const lbClose   = document.getElementById('lightbox-close');
  const lbPrev    = document.getElementById('lightbox-prev');
  const lbNext    = document.getElementById('lightbox-next');

  let currentIndex = 0;

  function getVisibleItems() {
    return [...document.querySelectorAll('.gallery-item:not(.hidden-item)')];
  }

  function openLightbox(index) {
    currentIndex = index;
    const items = getVisibleItems();
    const item = items[currentIndex];
    const img = item.querySelector('img');
    lbImg.src = img.src;
    lbImg.alt = img.alt;
    lbCaption.textContent = item.querySelector('span') ? item.querySelector('span').textContent : '';
    lightbox.classList.add('open');
    document.body.style.overflow = 'hidden';
  }

  function closeLightbox() {
    lightbox.classList.remove('open');
    document.body.style.overflow = '';
  }

  function navigate(dir) {
    const items = getVisibleItems();
    currentIndex = (currentIndex + dir + items.length) % items.length;
    const item = items[currentIndex];
    const img = item.querySelector('img');
    lbImg.style.opacity = '0';
    setTimeout(() => {
      lbImg.src = img.src;
      lbImg.alt = img.alt;
      lbCaption.textContent = item.querySelector('span') ? item.querySelector('span').textContent : '';
      lbImg.style.opacity = '1';
    }, 150);
  }

  document.querySelectorAll('.gallery-item').forEach((item) => {
    item.addEventListener('click', () => {
      const visible = getVisibleItems();
      const idx = visible.indexOf(item);
      if (idx !== -1) openLightbox(idx);
    });
  });

  lbClose.addEventListener('click', closeLightbox);
  lbPrev.addEventListener('click', () => navigate(-1));
  lbNext.addEventListener('click', () => navigate(1));
  lightbox.addEventListener('click', (e) => { if (e.target === lightbox) closeLightbox(); });

  document.addEventListener('keydown', (e) => {
    if (!lightbox.classList.contains('open')) return;
    if (e.key === 'Escape') closeLightbox();
    if (e.key === 'ArrowLeft') navigate(-1);
    if (e.key === 'ArrowRight') navigate(1);
  });
</script>